#include <stdio.h>
#include <stdlib.h>

#include "functions.h"

int num_children(graph_p graph, char *sp, int order) {
	return -1;
}

char* most_diverse_specied(graph_p graph, char *sp) {
	return "";
}

char* lowest_common_ancestor(graph_p graph, char *sp1, char *sp2) {
	return "";
}